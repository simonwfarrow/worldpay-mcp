type: object
properties:
  href:
    type: string
  method:
    type: string
    default: POST
description: >-
  Supply the 3DS device data collectionReference (SessionId) to continue the
  payment. See [details](../3ds-actions/supply3dsdevicedata) including what to
  add in the request body.

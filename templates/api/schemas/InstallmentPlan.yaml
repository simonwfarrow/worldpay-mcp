required:
  - numberOfInstallments
type: object
properties:
  numberOfInstallments:
    type: integer
    description: Number of installments that the requested amount should be broken into.
    format: int32
description: >-
  Required only for `latinAmerica` installment type. Not allowed for `merchant`
  installment type.
